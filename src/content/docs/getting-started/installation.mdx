---
title: Install and Setup
description: Install Jean, connect your preferred backend, and complete the first-run setup.
icon: Download
---

Jean runs as a native desktop app and works with the CLI tools you already use.

You can use Claude CLI, Codex CLI, OpenCode, Cursor Agent, PI, Command Code, Grok, Kimi Code, or a mix of them. Git is required. GitHub CLI and a Linear API key are optional, but they unlock more of the workflow.

## What you need

- Git
- At least one backend: Claude CLI, Codex CLI, OpenCode, Cursor Agent, PI, Command Code, Grok, or Kimi Code
- A Jean install from Homebrew or the [coollabsio/jean releases](https://github.com/coollabsio/jean/releases)
- Optional: GitHub CLI for issues, PRs, workflows, and security context
- Optional: Linear personal API key for Linear issue flows

## Install Jean

<Tabs items={["Homebrew", "Download"]}>
  <Tab value="Homebrew">

```bash
brew tap coollabsio/jean
brew install --cask jean
```

  </Tab>
  <Tab value="Download">
    Download the latest desktop build from [coollabsio/jean/releases](https://github.com/coollabsio/jean/releases), then move it into your normal app flow for macOS, Windows, or Linux.
  </Tab>
</Tabs>

## Complete setup

When Jean opens for the first time it checks whether any supported backends are installed.

If none are available, the welcome screen guides you through setup before you can add projects.

In the setup flow you can:
- verify CLI installation status
- authenticate Claude CLI, Codex CLI, GitHub CLI, OpenCode, Cursor Agent, PI, Command Code, Grok, and Kimi Code
- choose where each CLI comes from when path-based or Jean-managed installs are available
- set your default backend, model, and other defaults
- decide whether you want Jean to open terminals and editors with your preferred apps

<Callout title="Good default">
If you are new to Jean, start with one backend you already trust and add the others later. The app is easier to learn when the first session has fewer moving parts.
</Callout>

## Optional tools

- **GitHub CLI** adds issue, PR, workflow, and security investigation flows.
- **Linear API key** lets Jean pull Linear issues into the new-session flow.
- **Custom Claude CLI provider profiles** let you route Claude CLI through alternate endpoints while keeping Jean's workflow intact.

<Callout title="Web access comes later">
Every Jean instance can expose Web Access (desktop Preferences or the `jean-server` binary). That is a runtime choice, not an install step. See the web access guide once setup is done — Tailscale or a similar private network is recommended for remote use.
</Callout>

## Next reads

- [Add your first project](/getting-started/add-your-first-project)
- [Start your first session](/getting-started/start-your-first-session)
- [Backends, models, and MCP](/core-concepts/backends-models-and-mcp)
- [Web access and headless](/manage/web-access-and-headless)
